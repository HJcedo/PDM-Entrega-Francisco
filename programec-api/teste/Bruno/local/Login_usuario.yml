info:
  name: Login_usuario
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost/programec-api/public/sessoes
  body:
    type: json
    data: |-
      {
        "email": "joao@email.com",
        "senha": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
