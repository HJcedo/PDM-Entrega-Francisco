info:
  name: Delete_usuario
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost/programec-api/endpoints/deletar_usuario.php
  body:
    type: form-urlencoded
    data:
      - name: id
        value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
